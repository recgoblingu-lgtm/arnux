# Hello Arnux

A minimal Arnux app example.

Change `manifest.js` to customize the app name, logo, color, description, permissions, dependencies, and changelog. Change `app.js` to add interactions.

This app uses only local storage and Arnux notifications.
